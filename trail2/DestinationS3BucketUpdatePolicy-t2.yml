Parameters:
  SourceAccountNumber:
    Type: String
  CrossAccountRoleArn:
    Type: String

Resources:
  DestinationS3Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: s3destinationaccountbucketvinrv2
      VersioningConfiguration:
        Status: Enabled

  DestinationBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref DestinationS3Bucket
      PolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              AWS: !Ref CrossAccountRoleArn
            Action:
              - s3:*
            Resource: !Join
              - ""
              - - "arn:aws:s3:::"
                - !Ref DestinationS3Bucket
                - "/*"
          - Effect: Allow
            Principal:
              AWS: !Sub "arn:aws:iam::${SourceAccountNumber}:root"
            Action:
              - s3:*
            Resource: !Join
              - ""
              - - "arn:aws:s3:::"
                - !Ref DestinationS3Bucket
                - "/*"
          - Effect: Allow
            Principal:
              AWS: !Ref CrossAccountRoleArn
            Action:
              - s3:*
            Resource: !Join
              - ""
              - - "arn:aws:s3:::"
                - !Ref DestinationS3Bucket

Outputs:
  DestinationBucketArn:
    Description: Arn of destination bucket
    Value: !GetAtt DestinationS3Bucket.Arn
    Export:
      Name: ArnofS3BucketDestinationv2
